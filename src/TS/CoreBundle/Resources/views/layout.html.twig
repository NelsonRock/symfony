<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{% block title %}TS Plataform {% endblock %}</title>
    {% block stylesheet %}
        <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css">
    {% endblock %}
</head>
<body>
    <div class="container">
        <div id="header" class="jumbotron">
            {% for message in app.session.flashbag.get('infoContact') %}
                <div class="alert alert-warning">
                    <a href="#" class="close" data-dismiss="alert">&times;</a>
                    {{ message }}
                </div>
            {% endfor %}
            <h1>Mi platafoma de anuncios</h1>
            <p>Este proyecto es propulsado por Symfony</p>                
            <p><a href="" class="btn btn-primary btn-lg">Participa a  MOOC >></a></p>
        </div>
        <div class="row">
            <div id="menu" class="col-md-3">
                <h3>Los anuncios</h3>
                <ul class="nav nav-pills nav-stacked">
                    <li><a href="{{ path('ts_core_homepage')}}">Home</a></li>
                    <li><a href="{{ path('ts_platform_home')}}">Anuncios</a></li>
                    <li><a href="{{ path('ts_platform_add')}}">Crear un anuncio</a></li>
                    <li><a href="{{ path('ts_core_contact')}}">Contact</a></li>
                </ul>
                <h4>Ultimos anuncios</h4>
                {{ render(controller("TSPlatformBundle:Advert:menu", {'limit':3 })) }}
            </div>
            <div id="content" class="col-md-9">
                {% block body %}
                {% endblock %}
            </div>
        </div>
        <hr>
        <footer>
            <p>El cielo es el limite {{ 'now'|date('Y')}} y mas alla </p>
        </footer>
    </div>
    {% block javascripts %}   
    <script src="//ajax.googleapis.com/ajax/libs/jquery/1.11.1/jquery.min.js"></script>
    <script src="//maxcdn.bootstrapcdn.com/bootstrap/3.2.0/js/bootstrap.min.js"></script>
    {% endblock %}
</body>
</html>